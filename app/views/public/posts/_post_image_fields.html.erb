<div class="nested-fields">
  <div class="card" style="background-color: #222222;">
    <div class="card-body">
      <%= f.label :image, "画像" %><br>
      <%= f.file_field :image, class: "form-control-file" %>
    </div>
    <% if f.object.image.attached? %>
    <p>編集前の画像です(新しく画像を指定しない場合は編集前の画像が適用されます)</p>
    <%= image_tag f.object.image, size: "100x100" %>
    <% end %>
    <div class="card-body">
      <%= f.text_area :text, class: "form-control", placeholder: "写真について入力してください" %>
    </div>
    <div class="card-footer text-right">
      <%= link_to_remove_association "削除", f, class: "btn btn-outline-danger" %>
    </div>
  </div>
</div>
